{
  "discord_token": "discord bot token for connection",
  "twitch_clientID": "twicth API app clientID",
  "rules": {
    "channelID": {
      "allowedStreamers": ["twitch_channel_name"],
      "_comment": "This is no needed but this is an example how the JSON will look"
    }
  }
}
